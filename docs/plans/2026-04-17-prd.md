# PRD — Batch Update, Friday 17 April 2026

**Status:** Planned for tonight's session
**Source:** `TODO.md` — Thursday 16 April 2026 playtest feedback
**Scope:** 22 items. Future-plan items (download package, native install, Portal Eggs) are explicitly deferred.
**Purpose:** Reference record of this batch so we have a trail of what shipped and why.

---

## Execution order (task group buckets)

Four buckets. Order matters: 1 → 2 → 3 → 4. The three "big" items (full pixel-art/Pokedex upgrade, story+gyms+trainers, move-mechanics review) sit in Bucket 1 and Bucket 4 — the data-level half of the move review goes first so later buckets can rely on it.

| # | Bucket | Items | Why grouped |
|---|---|---|---|
| 1 | **Battle correctness & data audit** | 1, 10, 11, 12, 14, 21 | All touch `src/data/moves.ts`, `src/data/pokemon.ts`, `src/core/battleStateMachine.ts`, `src/core/damageCalc.ts`. Independent of UI. Foundation for later buckets. |
| 2 | **Pokemon / Item UI consistency** | 7, 8, 9, 15, 16, 17, 18, 19, 20 | All touch the shared list/filter/tab pattern used in party, trader, mart, train screens. One shared filter-modal component unblocks most of them. |
| 3 | **Dungeon flow & rewards** | 6, 13 | Both add new post-encounter summary screens (area clear, trainer defeat). Share the same reward-payout component. |
| 4 | **Immersion: story, trainers, gyms, art, animations** | 2, 3, 4, 5, 22 | The big content push. Depends on Bucket 3 (trainer defeat screen) and Bucket 1 (move animations need correct move data). Largest net-new content load. |

---

## Bucket 1 — Battle correctness & data audit

### 1.1 TM compatibility reset to Gen 1 Bulbapedia (TODO #1)

- Replace current "TM type must match Pokemon type" heuristic with a per-species allow-list mirroring the [Bulbapedia Gen 1 TM/HM compatibility table](https://bulbapedia.bulbagarden.net/wiki/List_of_moves_that_can_be_taught_by_TMs_in_Generation_I).
- Example corrections:
  - **TM08 Body Slam** is learnable by ~50 species across many types, not just Normal-types.
  - **Clefairy** is famously compatible with almost every TM/HM — should reflect that.
- Keep the current TM set (no expansion to all 50 Gen 1 TMs in this batch).
- Data lives in `src/data/pokemon.ts` (per-species) or a new `src/data/tmCompatibility.ts` keyed `{speciesId: tmId[]}` (preferred — single source of truth).
- PokeMart purchasing flow, TM apply flow, and "can learn" checks all read from this table.

### 1.2 Move mechanics — full Gen 1 pass (TODO #10, #11)

One-pass implementation of every special-mechanic move currently in `src/data/moves.ts`. Each becomes a typed effect on the `MoveData` / resolved in `battleStateMachine.ts`.

| Mechanic | Moves | Behaviour |
|---|---|---|
| **HP drain** | Absorb, Mega Drain, Leech Life | Heal user for 50% of damage dealt |
| **Leech Seed** | Leech Seed | Grass-type only; each turn: target loses 1/8 max HP, user heals same amount; persists until switch/faint |
| **Bide** | Bide | Lock user for 2 turns storing damage taken; turn 3 unleash 2× stored damage |
| **Rollout** | Rollout | Power doubles each consecutive hit up to 5 turns; resets on miss, switch, or different move |
| **Counter** | Counter | Next-turn resolve: deals 2× the physical damage received this turn (Fighting-type, 0 base power) |
| **Semi-invulnerable turn 1** | Dig, Fly | Turn 1 user is off-screen + untargetable; turn 2 attack lands. Sprite hides during charge turn |
| **Charge turn 1** | Solar Beam, Skull Bash, Sky Attack | Turn 1 charge message, turn 2 attack |
| **OHKO** | Horn Drill, Fissure, Guillotine | Gen 1 formula: hits if user level ≥ target level, accuracy = 30% + (user_lvl − target_lvl); fails outright if target higher level |
| **Confusion self-hit** | (status effect) | 50% chance confused mon hits itself for a 40-power typeless physical |
| **Flinch** | Bite 10%, Stomp 30%, Headbutt 30%, Hyper Fang 10%, Low Kick 30%, Rolling Kick 30% | On hit, chance to cause target to skip their next action if it hasn't yet moved this turn |
| **Multi-hit** | Double Slap, Fury Attack, Pin Missile, Comet Punch, Bonemerang | Gen 1 distribution: 2 (37.5%), 3 (37.5%), 4 (12.5%), 5 (12.5%) |
| **Recoil** | Take Down, Double-Edge, Submission | User takes 25% of damage dealt back as recoil |
| **Crit-boost** | Slash, Razor Leaf, Karate Chop, Crabhammer | High crit rate (Gen 1: 8× base crit) |

Events added to `battleStateMachine.ts`:

- `move_charging` (solar beam / dig turn 1)
- `semi_invulnerable` (dig/fly — blocks incoming attacks)
- `bide_storing`, `bide_unleash`
- `counter_fired`
- `rollout_stacked` (with current multiplier)
- `drain_heal`, `leech_seed_tick`
- `flinched`
- `ohko_hit`, `ohko_failed`

All mechanics tested in `tests/` with deterministic RNG.

### 1.3 Move accuracy audit (TODO #12) ✅

- Compare every move in `src/data/moves.ts` against Bulbapedia Gen 1 accuracy column.
- Single commit update so diff is self-contained and reviewable.
- **Shipped:** accuracy values in `src/data/moves.ts` updated to Gen 1 Bulbapedia (Tackle 95, Slam 75, Take Down 85, Hyper Beam 90, Wrap 85, Sing 55, Lovely Kiss 75, Glare 75, Fury Attack 85, Comet Punch 85, Mega Punch 85, Mega Kick 75, Horn Drill 30, Fire Blast 85, Fire Spin 70, Hydro Pump 80, Clamp 75, Crabhammer 85, Razor Leaf 95, Sleep Powder 75, Stun Spore 75, Poison Powder 75, Leech Seed 90, Thunder 70, Rock Throw 65, Rock Slide 90, Bone Club 85, Bonemerang 90, Fissure 30, Hypnosis 60, Smog 70, Toxic 85, Poison Gas 55, Low Kick 90, Submission 80, Hi Jump Kick 90, Rolling Kick 85, Fly 95, Sky Attack 90, String Shot 95, Pin Missile 95, Megahorn 85, Blizzard 90). Miss logic added to `battleStateMachine.resolveAttack` with a new `MoveMissedEvent` — moves under 100 accuracy roll `rng >= accuracy/100` before damage, and on miss emit `move_missed` (HUD shows "attack missed!") and skip damage + status. New tests in `tests/battleStateMachine.test.ts` cover both hit and miss paths. GAME_DESIGN.md §3.3 describes the new accuracy/miss check.

### 1.4 Move deletion guard (TODO #14) ✅

- Cannot remove the last CD-0 move from a Pokemon.
- If the player tries, show a toast: *"Must keep at least one no-cooldown move."*
- Applied in Train screen's move management flow.
- **Shipped:** `src/scenes/MainMenuScene.ts` — both the Current-Moves FORGET button and the TM forget picker enforce `isLastMove` + `isLastCdZero` guards with toast feedback.

### 1.5 X-item boost scope fix (TODO #21) ✅

- `battleBoosts` currently leak into subsequent battles.
- New rules, enforced in `battleStateMachine.ts`:
  - Reset `battleBoosts` on **battle end** (victory, defeat, flee) for every roster member.
  - **Persist** boosts on switch-out/switch-in within the same battle.
  - **Clear** boosts on faint (including Pokemon revived mid-battle — they come back clean).
- Unit test coverage for all four transitions: apply → switch → switch back → retained; apply → faint → revive → cleared; apply → battle end → new battle → cleared.
- **Shipped:** new `clearBoosts` helper + `endBattle` private method in `src/core/battleStateMachine.ts`. Faint cascade clears boosts on both player and enemy before emitting `faint`. `start()` still resets player party for defense-in-depth on new battles. 4 new tests in `tests/battleStateMachine.test.ts` cover all four transitions.

---

## Bucket 2 — Pokemon / Item UI consistency

All screens using the Pokemon-list or Item-list pattern should behave identically. A **shared filter/tab component** is the foundation — build it once, use everywhere.

### 2.1 Shared filter modal (TODO #16)

- New `src/ui/FilterModal.ts` — overlay modal that lets the player pick a single type from the 15-type grid.
- Includes a **Reset all filters** button.
- Triggered from a "Type" pill button in any list screen.
- Reused in: party screen, Pokemon trader, Train screen, and (where applicable) Pokedex.

### 2.2 Pokemart pagination + tabs (TODO #7, #8)

- **Pagination:** prev/next arrows, max 10 items per page. Same visual pattern as the Buy Pokemon screen.
- **Category tabs** across the top, one per `ItemCategory`: Medicine · Field · Vitamin · Stone · Candy · Battle · TM.
- Tabs drive filtering; pagination resets on tab switch.

### 2.3 Items screen tabs + Eggs tab (TODO #8)

- Same tab/pagination pattern as PokeMart.
- Plus an **Eggs** tab (not present in PokeMart) listing owned `EggInstance`s with steps-remaining-to-hatch.

### 2.4 Pokemon detail screen (stats + moves) (TODO #9)

- Selecting a Pokemon from party / trader / Train shows a detail view with:
  - Pixel portrait (upgraded — see Bucket 4.1)
  - Base stats: HP / Atk / Def / Spd / Spec (with applied vitamin bonuses shown as `+N`)
  - Level, XP progress bar, types, evolution line hint
  - Full moveset — for each move: type chip, power, accuracy, cooldown, status effect chance, one-line description
- This screen is the anchor for the "Pokedex feels immersive" ask in TODO #2.

### 2.5 Train screen filters (TODO #15)

- Same filter controls as the party screen: level sort, type filter (via the shared modal), search.

### 2.6 Stone-evolution label fix (TODO #17)

- If a species `evolutionStone` exists but the player lacks the stone, its Train-screen card stays enabled but shows sub-label *"Evolves to {Target} with a special item"*.
- Applies to Pikachu, Vulpix, Growlithe, Eevee (Vaporeon/Jolteon/Flareon branches), Clefairy, Jigglypuff, etc.
- Don't name the stone — keep the discovery intact.

### 2.7 Train screen per-Pokemon alignment fix (TODO #18)

- Button + section-heading alignment is currently inconsistent. Audit spacing, rebuild with a single grid template.

### 2.8 Forgotten moves bucket (TODO #19)

- Add `forgottenMoves: string[]` to `BattlePokemon` (persisted in save).
- When a move is overwritten via "forget", push the moveId onto that list.
- New subsection in Train screen: **Forgotten Moves** — relearnable **free**, instantly, without using the paid move-training UI.
- **"1 new"** badge on the Train Pokemon list now counts **only genuinely new moves from levelling / evolution / TM**, never forgotten ones.

### 2.9 Train list sort prioritising evolutions + new moves (TODO #20)

- Default Train screen sort:
  1. Pokemon with an evolution available **right now** (level/stone met + player has stone)
  2. Pokemon with a newly-available move (levelled up past an unlock)
  3. Then: ascending by level

---

## Bucket 3 — Dungeon flow & rewards

### 3.1 Area-cleared reward screen (TODO #6)

- New `src/scenes/RoomClearScene.ts` (or modal in `MapScene`) triggered when the player steps onto the exit tile after clearing a room.
- Layout mirrors the Pokemon end-of-battle screen: **Area Cleared!** → XP gained → gold gained → any drops.
- **Drop table** (regular rooms):
  - 25% basic item — pooled from: Potion, Antidote, Paralyze Heal, Awakening, Repel, Escape Rope, X-Attack, X-Defense, X-Speed
  - 5% decent item — pooled from: Super Potion, Revive, Ether, Hyper Potion, random vitamin (Carbos/Iron/etc.)
  - 1% **Rare Egg**
  - 1% **Legendary Egg** — gated to World 5+
- Rolls are independent; a room can drop zero, one, or multiple rewards.
- Trainer/boss/gym rooms get a **guaranteed** decent item + egg chance (see 3.2).

### 3.2 Trainer-defeat screen + exit-tile fix (TODO #13)

- Today: beating a boss turns the red square green, player walks off, but a wild encounter on the way out can reset it — player is forced to refight. Broken.
- **Fix:** on trainer/boss/gym victory, instead of green-tile-walk-out, show a **Trainer Defeat** screen (same component as 3.1 but themed): defeated-trainer portrait, "You defeated {Trainer}!", XP, gold, **guaranteed decent item + 1% egg roll**.
- From that screen → the room is marked cleared and the player transitions straight to the next room / world-select / town (same flow that the current green-tile handoff intends).
- No more returnable red-tile state. Kills the regression class entirely.

---

## Bucket 4 — Immersion: story, trainers, gyms, pixel art, animations

All three "big" content pushes land here. Order within the bucket: 4.1 (art asset prep) → 4.2 (trainer/gym data) → 4.3 (pre/post battle scenes) → 4.4 (story overlay) → 4.5 (animations).

### 4.1 Pixel-art uplift + Pokedex immersion (TODO #2)

Full-scope pass on two fronts:

**Sprites (`src/sprites/pokemonSprites.ts`):**
- Retain the 16×16 battle grid for parity with existing BattleScene scaling.
- Add a **hi-detail variant** per species — 32×32 with richer palette + shading — used in:
  - Pokemon detail screen (2.4)
  - New Pokedex entry view
  - Trainer defeat and room-clear reward screens
- Generation function still runs at boot (no external assets rule preserved).

**Pokedex entry view:**
- Accessed from the main menu **Pokedex** item.
- Per-species layout: large hi-detail portrait, species name + number, types, base stats, evolution chain (with greyed future stages for unseen forms), flavor text from `src/data/pokedexEntries.ts`, first-seen level (from `seenPokemon`).
- Locked species show silhouette + "???" until first seen.

### 4.2 Gen-1-accurate gym rosters (TODO #4)

Final room (room 25) of each of the 8 worlds becomes a gym with the canonical team and themed encounter pool for rooms 20–24.

| World | Gym | Leader | Roster (species @ level) | Encounter pool theme |
|---|---|---|---|---|
| 1 | Pewter | **Brock** | Geodude, Onix | Rock |
| 2 | Cerulean | **Misty** | Staryu, Starmie | Water |
| 3 | Vermilion | **Lt. Surge** | Voltorb, Pikachu, Raichu | Electric |
| 4 | Celadon | **Erika** | Victreebel, Tangela, Vileplume | Grass |
| 5 | Fuchsia | **Koga** | Koffing, Muk, Koffing, Weezing | Poison |
| 6 | Saffron | **Sabrina** | Kadabra, Mr. Mime, Venomoth, Alakazam | Psychic |
| 7 | Cinnabar | **Blaine** | Growlithe, Ponyta, Rapidash, Arcanine | Fire |
| 8 | Viridian | **Giovanni** | Rhyhorn, Dugtrio, Nidoqueen, Nidoking, Rhydon | Ground |

- **Levels** scale with the player's current world progression, not pinned to canon low numbers (Brock at level 12 would be trivial by world 1 room 25 in this game). Use the existing world-scaling formula applied to the canon *team size* and *relative level ordering*.
- Encounter pool for rooms 20–24 of each world is overridden to the gym's type (rock-only leading into Brock, etc.) to match the "final room should have all rock encounters because it is part of the gym" ask.

### 4.3 Trainer battle rework + pre-battle screen (TODO #5)

- Rename every UX mention of "Boss battle" → "Trainer battle".
- Trainer rooms at 5 / 10 / 15 / 20 (room 25 is the gym, covered in 4.2).
- **Pre-battle screen:** trainer portrait (pixel art), name, one-line dialogue, their first Pokemon silhouette. Player taps to continue → battle starts.
- **Post-battle screen:** covered by 3.2 (trainer defeat screen).
- Trainer roster expands from "one rare out-of-pool species" to a small canon-flavoured team (2–3 Pokemon) — Youngster/Lass/Bug Catcher/Hiker/Rocket Grunt flavour depending on world.
- New `src/data/trainers.ts` keyed by `trainerId` with: name, class, sprite key, dialogue, team (species + level offset).

### 4.4 Story intro overlay (TODO #3)

- Opening narrative on new game start: short scripted sequence (3–4 dialogue beats) establishing premise — trainer beginning their gauntlet, gym circuit to the champion.
- Per-world entry beat: one-line flavor text when you first enter each world ("You arrive at the outskirts of {Town}. The {Gym} gym looms ahead.").
- Pre-gym screen extends the pre-trainer screen with extra story context.
- Post-gym victory: "badge earned" beat with badge iconography (data-model addition: `badges: string[]` on `GameState`).

### 4.5 Move animations by type (TODO #22)

Phase-1 (**tonight**): one animation per type, reused across all moves of that type.

- Fire — rising flame burst
- Water — splash arc
- Electric — forked bolt
- Grass — leaf spiral
- Rock / Ground — projectile + dust
- Psychic — ripple distortion
- Ice — shard spray
- Normal / Fighting — impact flash
- Poison — dripping cloud
- Bug — scatter
- Ghost — fade-pulse
- Flying — wind streak
- Dragon — energy arc

Phase-2 (deferred, tracked in TODO): per-move custom animations.

---

## Out of scope (future plans — unchanged)

Kept in `TODO.md` as prompts for future exploration, **not** built tonight:

1. Downloadable package for local run.
2. Native install (non-browser) — save-system implications make this a large rework.
3. Portal Eggs introducing new species from the other pokemon game (e.g. bogwarden) as pixel art.

---

## Done-definition for the batch

For tonight to be "done":

- [ ] All 22 items above merged to `main` and pushed.
- [ ] `npm test` green.
- [ ] `CHANGELOG.md` has entries for today (2026-04-17) grouped by bucket.
- [ ] `GAME_DESIGN.md` updated wherever gameplay/balance/content changed (gyms, trainer rework, drop tables, move mechanics, X-item scope, TM compatibility).
- [ ] `CLAUDE.md` directory structure updated if new files added (`tmCompatibility.ts`, `trainers.ts`, `RoomClearScene.ts`, `FilterModal.ts`).

---

## Open questions flagged during spec (answer as we implement)

These don't block starting Bucket 1, but I'll need answers by the time we reach the flagged item:

- **Bucket 4.2 — gym levels:** confirm scaling formula matches normal world progression (player-relative), **not** pinned to canon Gen 1 levels. If you want canon levels preserved for nostalgia, we'd need to rescale the whole difficulty curve.
- **Bucket 4.3 — trainer teams:** use generic Gen 1 trainer classes (Youngster, Lass, Hiker, etc.) with flavour names, or invent our own trainer identities?
- **Bucket 4.4 — badges:** do gym wins award a collectible badge (and is there a Badge screen in the menu)?
- **Bucket 2.8 — forgotten moves UI location:** within the existing Train screen as a new subsection, or its own top-level menu item?
- **Bucket 3.1 — legendary egg gating:** confirm World 5+ is the right threshold (currently proposed), or earlier/later?
